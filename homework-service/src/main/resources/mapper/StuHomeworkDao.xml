<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuan.homeworkservice.dao.StuHomeworkDao">

    <resultMap type="com.yuan.homeworkservice.entity.StuHomework" id="StuHomeworkMap">
        <result property="stuHomId" column="stu_hom_id" jdbcType="INTEGER"/>
        <result property="homId" column="hom_id" jdbcType="INTEGER"/>
        <result property="stuId" column="stu_id" jdbcType="INTEGER"/>
        <result property="stuHomScore" column="stu_hom_score" jdbcType="VARCHAR"/>
        <result property="stuHomRank" column="stu_hom_rank" jdbcType="INTEGER"/>
        <result property="stuHomCommit" column="stu_hom_commit" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="stuHomId" useGeneratedKeys="true">
        insert into bishe.stu_homework(hom_id, stu_id, stu_hom_score, stu_hom_rank, stu_hom_commit)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.homId}, #{entity.stuId}, #{entity.stuHomScore}, #{entity.stuHomRank}, #{entity.stuHomCommit})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="stuHomId" useGeneratedKeys="true">
        insert into bishe.stu_homework(hom_id, stu_id, stu_hom_score, stu_hom_rank, stu_hom_commit)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.homId}, #{entity.stuId}, #{entity.stuHomScore}, #{entity.stuHomRank}, #{entity.stuHomCommit})
        </foreach>
        on duplicate key update
        hom_id = values(hom_id) , stu_id = values(stu_id) , stu_hom_score = values(stu_hom_score) , stu_hom_rank =
        values(stu_hom_rank) , stu_hom_commit = values(stu_hom_commit)
    </insert>

</mapper>

